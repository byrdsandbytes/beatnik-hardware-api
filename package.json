{
  "name": "beatnik-hardware-api",
  "version": "0.6.0",
  "description": "Microservice to configure Raspberry Pi Hardware & CamillaDSP for Beatnik",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "pack-pi": "npm run build && tar -czvf beatnik-hardware-api.tar.gz dist package.json package-lock.json"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.24.3",
    "js-yaml": "^4.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.8.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
